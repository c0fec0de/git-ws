Dependency Resolution
=====================

TODO: Document the dependency resolution approach we use. In particular, show how to "override" the version of a dependency by specifying it again in the main project's manifest.